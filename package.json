{
  "name": "luxor-hub-monorepo",
  "private": true,
  "scripts": {
    "build": "npm --prefix luxor-hub run build",
    "dev": "npm --prefix luxor-hub run dev",
    "preview": "npm --prefix luxor-hub run preview"
  }
}