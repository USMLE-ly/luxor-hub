{
  "builds": [
    {
      "src": "main.py",
      "use": "@vercel/python"
    },
    {
      "src": "luxor-hub/package.json",
      "use": "@vercel/static-build",
      "config": {
        "installCommand": "cd luxor-hub && npm install --ignore-scripts",
        "buildCommand": "cd luxor-hub && sh ../build.sh",
        "outputDirectory": "../dist"
      }
    }
  ],
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/main.py" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
