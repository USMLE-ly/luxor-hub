{
  "name": "luxor-hub",
  "private": true,
  "scripts": {
    "build": "cd luxor-hub && npm install && npm run build",
    "dev": "cd luxor-hub && npm install && npm run dev",
    "preview": "cd luxor-hub && npm run preview"
  },
  "overrides": {
    "rollup": "4.21.3"
  }
}
